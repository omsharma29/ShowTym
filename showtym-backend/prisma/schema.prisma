// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider        = "prisma-client-js"
  output          = "../src/generated/prisma"
  previewFeatures = ["driverAdapters"] // 👈 required for Neon in Workers
}


datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Booking {
  id           String   @id @default(uuid())
  clerkId      String
  email        String
  movieName    String
  movieId      String   // ✅ new field
  City         String
  Address      String
  ShowDate     DateTime
  ShowTime     String
  totalSeat    Int
  totalPaid    Float
  bookingDate  DateTime @default(now())
  seatNos      String[]   // ✅ array of seat numbers (Postgres only)
  orderId       String
  PaymentDone  Boolean


  @@index([clerkId])
   @@index([orderId])
  @@index([email])
}
